# PDF Chunking

This folder contains a sample PDF and the resulting chunks after processing it.

* `0097-pdf.pdf` — original PDF
* `test_chunk_02.txt` — chunked output from the PDF
* `ingestion/` — code used for PDF processing and chunking

The purpose is simply to show how the PDF is being converted into chunks.
